apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: model-volume-claim
  annotations:
    nfs.io/storage-path: "model-volume-claim"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
